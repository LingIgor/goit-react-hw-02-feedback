import styled from 'styled-components';

export const List = styled.ul`
  font-size: 40px;
  list-style: none;
  background: linear-gradient(150deg, green 10%, yellow 20%, blue 50%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  display: block;
  padding: 0.3em 0.6em;
`;
